node_modules/
.DS_Store
Thumbs.db
.claude/

# 临时构建脚本与中间产物（_ 前缀），运行时不需要
_*

# 旧的外链图片映射（已被本地缓存取代）
images.json

# 图片已迁至独立仓 xujiann/1001art-img，经 jsDelivr 分发。
# 本地 images/ 仅作构建缓存与推送镜像仓的源，不再随主仓/Pages 发布。
/images/
